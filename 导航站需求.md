这是一个**完整的、基于中文指令的系统级开发方案**。

你可以直接将下面框内的内容**全部复制**，发送给 AI（如 ChatGPT, Claude, Cursor 等）。它包含了从项目结构、后端抓取脚本、自动化工作流到前端组件的所有详细指令。

---

### 📋 AI 开发指令 (请复制以下内容)

**角色设定**: 你是一位精通 React 和 Node.js 的全栈工程师，擅长构建“极简主义”风格的 Web 应用。

**项目名称**: The Daily Hub (每日聚合页)
**项目描述**: 一个符合欧美用户使用习惯的浏览器“新标签页”。它结合了高效的命令搜索栏、可自定义的快捷图标、以及自动更新的科技新闻流。
**核心架构策略**: **"Git-Scraping" (无数据库模式)**。我们不使用传统数据库，而是利用 GitHub Actions 定时运行 Node.js 脚本抓取外部 API（Hacker News, Reddit），生成静态 JSON 文件并提交回代码仓库。前端直接读取这个静态 JSON 文件。

---

### 1. 技术栈与规范

*   **前端**: React (Vite 构建), Tailwind CSS, Lucide-React (图标库)。
*   **数据生成**: Node.js (Axios, fs)。
*   **自动化**: GitHub Actions。
*   **状态管理**: `localStorage` (用于存储用户的快捷方式、暗黑模式偏好)。
*   **设计风格**: 类似 Notion 的极简风。大留白、圆角卡片 (`rounded-2xl`)、Slate 色系 (`slate-50` 到 `slate-950`)。

---

### 2. 数据接口约定 (Data Contract)

Node.js 脚本必须在 `public/feeds-data.json` 生成完全符合以下 Schema 的数据，以便前端读取：

```json
{
  "updated_at": "2023-10-27T10:00:00Z",
  "tech": [
    { "id": "1", "title": "HN 热点新闻标题", "url": "...", "source": "HN", "points": 150 }
  ],
  "design": [
    { "id": "2", "title": "Product Hunt 产品名", "url": "...", "source": "PH", "points": 45 }
  ],
  "news": [
    { "id": "3", "title": "Reddit 热帖标题", "url": "...", "source": "Reddit", "points": 1200 }
  ]
}
```

---

### 3. 逐步实现指令 (请按此顺序生成代码)

#### 第一步：项目结构 (Project Structure)
请搭建以下文件结构：
```text
root/
├── .github/workflows/
│   └── update-feeds.yml    # 自动化工作流配置
├── scripts/
│   └── fetch-feeds.js      # 数据抓取脚本
├── public/
│   └── feeds-data.json     # 生成的静态数据（初始化为空或 Mock）
├── src/
│   ├── components/         # React 组件 (Header, SearchBar, SpeedDial, FeedSection)
│   ├── hooks/              # useLocalStorage.js
│   └── App.jsx
└── package.json
```

#### 第二步：数据抓取脚本 (`scripts/fetch-feeds.js`)
编写一个 Node.js 脚本，完成以下逻辑：
1.  使用 `axios` 并发请求数据：
    *   **Hacker News**: 访问 `https://hacker-news.firebaseio.com/v0/topstories.json` (取前 15 个 ID，再遍历获取详情 `item/{id}.json` 以得到标题/URL/分数)。
    *   **Reddit (Tech)**: 访问 `https://www.reddit.com/r/technology/top.json?limit=10&t=day`。
    *   **Design 类 (模拟)**: 由于 Product Hunt API 需要复杂验证，请生成 5 条静态的模拟数据，或者抓取一个无需验证的 RSS JSON 源。
2.  **数据清洗**: 将所有来源的数据统一转换为上述 **数据接口约定** 的格式。
3.  **写入文件**: 将结果写入 `./public/feeds-data.json`。
4.  **容错处理**: 如果某个 API 请求失败，该板块返回空数组，不要让整个脚本崩溃。

#### 第三步：自动化工作流 (`.github/workflows/update-feeds.yml`)
编写 GitHub Action 配置文件：
1.  **触发条件**: 定时任务 (`cron: '*/30 * * * *'`，每30分钟) 以及 手动触发 (`workflow_dispatch`)。
2.  **环境**: `ubuntu-latest`，安装 Node.js。
3.  **步骤**:
    *   Checkout 代码。
    *   `npm install axios` (临时安装依赖，或使用 `npx`)。
    *   运行 `node scripts/fetch-feeds.js`。
    *   **关键步骤**: 检查 `public/feeds-data.json` 是否有变化。如果有变化，使用 git 命令将变更提交并 Push 回仓库（Commit message: "chore: update feeds data"）。

#### 第四步：前端组件开发

**A. 全局样式与布局**
*   实现亮色/暗色模式切换 (Dark Mode Toggle)。
*   背景色：亮色用 `bg-slate-50`，暗色用 `bg-slate-950`。

**B. 核心搜索组件 (`SearchBar.jsx`)**
*   位于屏幕正中央，大尺寸圆角输入框，带阴影。
*   **实现 "Bang" 命令逻辑**:
    *   输入以 `yt ` 开头 -> 跳转 YouTube 搜索。
    *   输入以 `gh ` 开头 -> 跳转 GitHub 搜索。
    *   输入以 `r/ ` 开头 -> 跳转 Reddit 对应板块 (如 `reddit.com/r/ai`)。
    *   默认 -> 跳转 Google 搜索。

**C. 快捷拨号组件 (`SpeedDial.jsx`)**
*   显示一个网格的快捷图标。
*   数据存储在 `localStorage` 中 (Key: `dailyhub_shortcuts`)。
*   **功能**:
    *   默认预置：Gmail, YouTube, ChatGPT, GitHub。
    *   支持用户**添加**（简单的弹窗输入 Name/URL）和**删除**。

**D. 信息流组件 (`FeedSection.jsx`)**
*   使用 `useEffect` 在组件挂载时 `fetch('/feeds-data.json')`。
*   实现 Tab 切换界面：[ Tech | Design | News ]。
*   **UI 细节**: 显示新闻标题、来源标签 (Source Tag)、热度 (Points)，点击在新标签页打开。
*   底部显示“上次更新时间”。

---

### 4. 执行请求

请先从 **后端逻辑** 开始（即 Step 2 的脚本和 Step 3 的工作流），然后再生成 **前端 React 代码**。请提供完整的、可直接使用的代码块。

---

### 💡 如何使用这份方案

1.  **发送指令**：将上面框内的中文全部发送给 AI。
2.  **搭建项目**：
    *   在本地运行 `npm create vite@latest my-daily-hub -- --template react`。
    *   安装依赖：`npm install axios lucide-react class-variance-authority clsx tailwind-merge` (以及 Tailwind 的初始化)。
3.  **填入代码**：把 AI 生成的脚本放入 `scripts/`，把组件放入 `src/components/`。
4.  **测试抓取**：
    *   在本地终端运行 `node scripts/fetch-feeds.js`。
    *   观察 `public/` 目录下是否生成了 json 文件。
5.  **启动前端**：`npm run dev`，你将看到一个带有真实数据的、极简主义的欧美风导航站。